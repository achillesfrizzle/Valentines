<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Valentine 💖</title>

<style>
  * {
    box-sizing: border-box;
  }

  body {
    margin: 0;
    height: 100vh;
    font-family: 'Comic Sans MS', cursive;
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .card {
    background: white;
    width: 90%;
    max-width: 360px;
    padding: 30px 20px;
    border-radius: 20px;
    text-align: center;
    box-shadow: 0 10px 25px rgba(0,0,0,0.2);
    position: relative;
  }

  h1 {
    color: #ff4d6d;
    font-size: 24px;
    margin-bottom: 10px;
  }

  p {
    font-size: 16px;
    margin-bottom: 25px;
  }

  .buttons {
    position: relative;
    height: 140px;
  }

  button {
    padding: 12px 26px;
    font-size: 16px;
    border-radius: 30px;
    border: none;
    cursor: pointer;
    transition: transform 0.2s ease;
  }

  #yesBtn {
    background: #ff4d6d;
    color: white;
  }

  #noBtn {
    background: #ddd;
    position: absolute;
  }
</style>
</head>

<body>

<div class="card">
  <h1>💘 Will You Be My Valentine?</h1>
  <p>May tamang sagot… piliin mo na 😌</p>

  <div class="buttons">
    <button id="yesBtn">YES 💖</button>
    <button id="noBtn">NO 😒</button>
  </div>
</div>

<script>
  const noBtn = document.getElementById("noBtn");
  const yesBtn = document.getElementById("yesBtn");

  const MAX_CLICKS = 12; // pwede 10–15, ginawa nating 12
  let clickCount = 0;

  let noScale = 1;
  let yesScale = 1;

  function moveNoButton() {
    const x = Math.random() * (window.innerWidth - noBtn.offsetWidth);
    const y = Math.random() * (window.innerHeight - noBtn.offsetHeight);

    noBtn.style.left = x + "px";
    noBtn.style.top = y + "px";
  }

  // initial position ng NO
  moveNoButton();

  noBtn.addEventListener("click", () => {
    clickCount++;

    // NO button: lumiliit + tumatakas
    noScale = 1 - (clickCount / MAX_CLICKS);
    if (noScale <= 0.05) noScale = 0.05;
    noBtn.style.transform = `scale(${noScale})`;
    moveNoButton();

    // YES button: lumalaki, same place
    yesScale = 1 + (clickCount / MAX_CLICKS) * 2;
    yesBtn.style.transform = `scale(${yesScale})`;

    // kapag naabot na max clicks
    if (clickCount >= MAX_CLICKS) {
      noBtn.style.display = "none";
    }
  });

  yesBtn.addEventListener("click", () => {
    document.body.innerHTML = `
      <div style="
        height:100vh;
        display:flex;
        flex-direction:column;
        align-items:center;
        justify-content:center;
        background:linear-gradient(135deg,#ff758c,#ff7eb3);
        color:white;
        text-align:center;
        font-family:'Comic Sans MS', cursive;
        padding:20px;
      ">
        <h1>AYIEEEEEE 😝😝😝</h1>
		<p style="font-size:22px;">Good Choice kaaaa😝</p>
        <p style="font-size:22px;">I LOVE YOUUUU MAHALLLL😘😘</p>
        <p>Happy Valentine’s Day 🌹</p>
      </div>
    `;
  });
</script>

</body>
</html>
